# Extractable Components

No extractable shared layout/basic components were identified yet.

- Current UI is mostly inline within `app/page.tsx`.
- Recommended next step: extract reusable sections into `app/components/` (e.g., `TopNav`, `HeroSection`, `StatsGrid`, `SentimentCard`) before component extraction through SuperDesign.
